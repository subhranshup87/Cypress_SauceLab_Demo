{
  "name": "saucedemo-smoke-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test:chrome": "cypress run --browser chrome",
    "report:generate": "allure generate allure-results --clean -o allure-report",
    "report:open": "allure open allure-report",
    "clean": "rm -rf allure-results allure-report cypress/videos cypress/screenshots",
    "test": "npm run clean && npm run test:chrome && npm run report:generate && npm run report:open"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.7",
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "allure-cypress": "^3.4.2",
    "cypress": "^12.14.0"
  }
}
